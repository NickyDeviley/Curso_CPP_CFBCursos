#include <iostream>
#include "POO2.h"   // ADICIONANDO O ARQUIVO.H

using namespace std;

// AULA 42
int main(int argc, char *argv[]) {

    /*
        A linguagem C e C++ possuem os arquivos de cabeçalho.
        É uma forma útil de organizar o código. Dentro desses
        arquivos nós podemos declarar classes e métodos para
        serem utilizados. Também podemos apenas prototipar
        estas classes e métodos e declarar eles em arquivos
        próprios. Isso facilita muito a organização.

        Também é apresentado o construtor. Trata-se
        de uma maneira para declarar os objetos com
        as variáveis iniciais.
        
        Podemos criar uma sobrecarga de construtores para
        facilitar ainda mais a declaração de objetos.
    */

    // DECLARANDO O OBJETO:
    Cachorro dog{};

    return 0;
}